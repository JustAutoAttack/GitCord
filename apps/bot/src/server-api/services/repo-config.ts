import { AppError, ErrorCode, serverAPILogger } from '@core';
import { apiClient } from '../client';

export interface IServerAPIRepoConfigService {}

export const ServerAPIRepoConfigService: IServerAPIRepoConfigService = {

    
};
